;; nested_iterator.synth — V_outer holds V_inner's; consumer does
;; outer.__getitem__(0).__getitem__(0). Two distinct vector
;; types + method dispatch through __getitem__/__setitem__.
;; Corrected version per PRIMITIVES §13.12 — see issue 007
;; iteration 10 for the failed raw-primitive attempt.

shape: nested_iterator
